# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Gemini API Configuration (REQUIRED)
GEMINI_API_KEY=your_gemini_api_key_here

# MinIO Configuration (optional - defaults are set in config.py)
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=password123
MINIO_BUCKET=legal-docs
MINIO_SECURE=false

# Kafka Configuration (optional - defaults are set in config.py)
KAFKA_BOOTSTRAP_SERVERS=redpanda:9092
KAFKA_TOPIC_PREFIX=legal

# Database Configuration (optional - defaults are set in config.py)
DATABASE_URL=postgresql://legal_user:legal_password_123@postgres:5432/legal_docs_dev

# Application Configuration (optional - defaults are set in config.py)
LOG_LEVEL=INFO
APP_HOST=0.0.0.0
APP_PORT=8001
